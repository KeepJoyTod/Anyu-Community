<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mapper.ElectricityRechargeRecordMapper">
    <insert id="insert" parameterType="com.ruoyi.domain.ElectricityRechargeRecord" useGeneratedKeys="true" keyProperty="id">
        insert into electricity_recharge_record (user_id, amount, electricity_added, pay_method, status, create_time, update_time)
        values (#{userId}, #{amount}, #{electricityAdded}, #{payMethod}, #{status}, #{createTime}, #{updateTime})
    </insert>
</mapper>
