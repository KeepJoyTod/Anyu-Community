<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mapper.WaterRechargeRecordMapper">
    <insert id="insert" parameterType="com.ruoyi.domain.WaterRechargeRecord" useGeneratedKeys="true" keyProperty="id">
        insert into water_recharge_record (user_id, amount, water_added, pay_method, status, create_time, update_time)
        values (#{userId}, #{amount}, #{waterAdded}, #{payMethod}, #{status}, #{createTime}, #{updateTime})
    </insert>
</mapper>
